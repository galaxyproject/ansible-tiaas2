Static files placed in here will be available to reference in your custom HTML templates.

e.g. `./footer/galaxyproject.png` can be referenced like:

```html
<img width="200" src="{% static 'footer/galaxyproject.png' %}" />
```
